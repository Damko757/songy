import { YTMUSIC } from "ytmusic";
// https://www.npmjs.com/package/ytmusic
export class YTMusic {
  constructor() {
    // YT Music API from npm is now deprecated. Python lib to be used instead via bridge
  }

  search(what: string) {
    // return this.api.search("Hot stuff");
  }
}
